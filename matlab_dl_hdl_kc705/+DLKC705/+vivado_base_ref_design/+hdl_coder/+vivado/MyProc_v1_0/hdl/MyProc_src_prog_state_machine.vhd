-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_prog_state_machine.vhd
-- Created: 2025-10-22 15:46:42
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_prog_state_machine
-- Source Path: testbench/DUT/DUT/FC Kernel/Real FC Kernel/FCEngine/FC LC Module/Prog/prog_state_machine
-- Hierarchy Level: 7
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_prog_state_machine IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        start                             :   IN    std_logic;
        progDone                          :   IN    std_logic;
        moveOn                            :   IN    std_logic_vector(1 DOWNTO 0);  -- ufix2
        fifo_not_empty                    :   IN    std_logic;
        load                              :   OUT   std_logic;
        stage                             :   OUT   std_logic;
        progStart                         :   OUT   std_logic;
        pop_fifo                          :   OUT   std_logic;
        stateOut                          :   OUT   std_logic_vector(3 DOWNTO 0);  -- ufix4
        loadingCounterOut                 :   OUT   std_logic_vector(4 DOWNTO 0)  -- ufix5
        );
END MyProc_src_prog_state_machine;


ARCHITECTURE rtl OF MyProc_src_prog_state_machine IS

  -- Signals
  SIGNAL moveOn_unsigned                  : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL stateOutdeadOut                  : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL loadingCounterOutdeadOut         : unsigned(4 DOWNTO 0);  -- ufix5
  SIGNAL loadingCounter                   : unsigned(4 DOWNTO 0);  -- ufix5
  SIGNAL state                            : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL loadingCounter_next              : unsigned(4 DOWNTO 0);  -- ufix5
  SIGNAL state_next                       : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL stateOut_tmp                     : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL loadingCounterOut_tmp            : unsigned(4 DOWNTO 0);  -- ufix5

BEGIN
  moveOn_unsigned <= unsigned(moveOn);

  dnnfpgaSyncedprocessorlib_c2_sB2BJAX9c0szQxVFlNc93ZH_prog_state_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      loadingCounter <= to_unsigned(16#00#, 5);
      state <= to_unsigned(16#0#, 4);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        loadingCounter <= loadingCounter_next;
        state <= state_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaSyncedprocessorlib_c2_sB2BJAX9c0szQxVFlNc93ZH_prog_state_process;

  dnnfpgaSyncedprocessorlib_c2_sB2BJAX9c0szQxVFlNc93ZH_prog_state_output : PROCESS (fifo_not_empty, loadingCounter, moveOn_unsigned, progDone, start, state)
    VARIABLE loadingCounter_temp : unsigned(4 DOWNTO 0);
    VARIABLE state_temp : unsigned(3 DOWNTO 0);
    VARIABLE add_temp : unsigned(5 DOWNTO 0);
    VARIABLE add_temp_0 : unsigned(5 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(16#00#, 6);
    add_temp_0 := to_unsigned(16#00#, 6);
    loadingCounter_temp := loadingCounter;
    state_temp := state;
    -- state difinition
    -- State Memory Encoding
    -- when moveOn is 2, reset
    IF moveOn_unsigned = to_unsigned(16#2#, 2) THEN 
      state_temp := to_unsigned(16#1#, 4);
      loadingCounter_temp := to_unsigned(16#00#, 5);
    END IF;
    -- output
    loadingCounterOutdeadOut <= loadingCounter_temp;
    -- state definition
    CASE state_temp IS
      WHEN "0000" =>
        load <= '0';
        stage <= '0';
        progStart <= '0';
        pop_fifo <= '0';
        loadingCounter_temp := to_unsigned(16#00#, 5);
      WHEN "0001" =>
        stage <= '0';
        progStart <= '0';
        IF fifo_not_empty = '1' THEN 
          load <= '1';
          add_temp := resize(loadingCounter_temp, 6) + to_unsigned(16#01#, 6);
          IF add_temp(5) /= '0' THEN 
            loadingCounter_temp := "11111";
          ELSE 
            loadingCounter_temp := add_temp(4 DOWNTO 0);
          END IF;
          pop_fifo <= '1';
        ELSE 
          load <= '0';
          pop_fifo <= '0';
        END IF;
      WHEN "0010" =>
        load <= '0';
        stage <= '0';
        progStart <= '0';
        pop_fifo <= '0';
        loadingCounter_temp := to_unsigned(16#00#, 5);
      WHEN "0011" =>
        load <= '0';
        stage <= '1';
        progStart <= '1';
        pop_fifo <= '0';
      WHEN "0100" =>
        load <= '0';
        stage <= '0';
        progStart <= '0';
        pop_fifo <= '0';
      WHEN "0101" =>
        stage <= '0';
        progStart <= '0';
        IF fifo_not_empty = '1' THEN 
          load <= '1';
          add_temp_0 := resize(loadingCounter_temp, 6) + to_unsigned(16#01#, 6);
          IF add_temp_0(5) /= '0' THEN 
            loadingCounter_temp := "11111";
          ELSE 
            loadingCounter_temp := add_temp_0(4 DOWNTO 0);
          END IF;
          pop_fifo <= '1';
        ELSE 
          load <= '0';
          pop_fifo <= '0';
        END IF;
      WHEN "0110" =>
        load <= '0';
        stage <= '0';
        progStart <= '0';
        pop_fifo <= '0';
        loadingCounter_temp := to_unsigned(16#00#, 5);
      WHEN "0111" =>
        load <= '0';
        stage <= '0';
        progStart <= '0';
        pop_fifo <= '0';
      WHEN OTHERS => 
        load <= '0';
        stage <= '0';
        progStart <= '0';
        pop_fifo <= '0';
        loadingCounter_temp := to_unsigned(16#00#, 5);
    END CASE;
    -- next state transition
    CASE state_temp IS
      WHEN "0000" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#1#, 4);
        ELSE 
          state_next <= to_unsigned(16#0#, 4);
        END IF;
      WHEN "0001" =>
        IF loadingCounter_temp = to_unsigned(16#1F#, 5) THEN 
          state_next <= to_unsigned(16#2#, 4);
        ELSE 
          state_next <= to_unsigned(16#1#, 4);
        END IF;
      WHEN "0010" =>
        IF moveOn_unsigned /= to_unsigned(16#0#, 2) THEN 
          state_next <= to_unsigned(16#3#, 4);
        ELSE 
          state_next <= to_unsigned(16#2#, 4);
        END IF;
      WHEN "0011" =>
        state_next <= to_unsigned(16#4#, 4);
      WHEN "0100" =>
        state_next <= to_unsigned(16#5#, 4);
      WHEN "0101" =>
        IF loadingCounter_temp = to_unsigned(16#1F#, 5) THEN 
          state_next <= to_unsigned(16#6#, 4);
        ELSE 
          state_next <= to_unsigned(16#5#, 4);
        END IF;
      WHEN "0110" =>
        IF (moveOn_unsigned /= to_unsigned(16#0#, 2)) AND progDone = '1' THEN 
          state_next <= to_unsigned(16#3#, 4);
        ELSIF moveOn_unsigned /= to_unsigned(16#0#, 2) THEN 
          state_next <= to_unsigned(16#7#, 4);
        ELSIF progDone = '1' THEN 
          state_next <= to_unsigned(16#2#, 4);
        ELSE 
          state_next <= to_unsigned(16#6#, 4);
        END IF;
      WHEN "0111" =>
        IF progDone = '1' THEN 
          state_next <= to_unsigned(16#3#, 4);
        ELSE 
          state_next <= to_unsigned(16#7#, 4);
        END IF;
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 4);
    END CASE;
    stateOutdeadOut <= state_temp;
    loadingCounter_next <= loadingCounter_temp;
  END PROCESS dnnfpgaSyncedprocessorlib_c2_sB2BJAX9c0szQxVFlNc93ZH_prog_state_output;


  stateOut_tmp <= to_unsigned(16#0#, 4);

  stateOut <= std_logic_vector(stateOut_tmp);

  loadingCounterOut_tmp <= to_unsigned(16#00#, 5);

  loadingCounterOut <= std_logic_vector(loadingCounterOut_tmp);

END rtl;

