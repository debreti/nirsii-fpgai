-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_DDR4_Burst_Conroller.vhd
-- Created: 2025-10-22 15:46:41
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_DDR4_Burst_Conroller
-- Source Path: testbench/DUT/DUT/FC Kernel/Real FC Kernel/FCEngine/FCDUT/StreamFIFO_DDRBurstControl/DDR4 Burst Conroller
-- Hierarchy Level: 7
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_DDR4_Burst_Conroller IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        length_in                         :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        start                             :   IN    std_logic;
        reset_1                           :   IN    std_logic;
        rd_dvalid                         :   IN    std_logic;
        rd_ack                            :   IN    std_logic;
        valid_out                         :   OUT   std_logic;
        count_out                         :   OUT   std_logic_vector(15 DOWNTO 0);  -- uint16
        ready_out                         :   OUT   std_logic;
        rd_addr                           :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        rd_len                            :   OUT   std_logic_vector(15 DOWNTO 0);  -- uint16
        rd_avalid                         :   OUT   std_logic;
        rd_ready                          :   OUT   std_logic;
        state_out                         :   OUT   std_logic_vector(3 DOWNTO 0);  -- ufix4
        Done                              :   OUT   std_logic
        );
END MyProc_src_DDR4_Burst_Conroller;


ARCHITECTURE rtl OF MyProc_src_DDR4_Burst_Conroller IS

  -- Functions
  -- HDLCODER_TO_STDLOGIC 
  FUNCTION hdlcoder_to_stdlogic(arg: boolean) RETURN std_logic IS
  BEGIN
    IF arg THEN
      RETURN '1';
    ELSE
      RETURN '0';
    END IF;
  END FUNCTION;


  -- Signals
  SIGNAL length_in_unsigned               : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL count_outdeadOut                 : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL rd_addr_tmp                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL rd_len_tmp                       : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL state_outdeadOut                 : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL counter                          : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL burst_stop                       : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL burst_count                      : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL state                            : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL reset_latch                      : std_logic;
  SIGNAL done_latch                       : std_logic;
  SIGNAL counter_next                     : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL burst_stop_next                  : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL burst_count_next                 : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL state_next                       : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL reset_latch_next                 : std_logic;
  SIGNAL done_latch_next                  : std_logic;
  SIGNAL count_out_tmp                    : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL state_out_tmp                    : unsigned(3 DOWNTO 0);  -- ufix4

BEGIN
  length_in_unsigned <= unsigned(length_in);

  dnnfpgaTblibMemcontrollerlib_c2_sbghByXoFHXTGWdFEAGFXsC_DDR4_Bu_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      counter <= to_unsigned(16#00000000#, 32);
      burst_stop <= to_unsigned(16#0000#, 16);
      burst_count <= to_unsigned(16#0000#, 16);
      state <= to_unsigned(16#0#, 4);
      reset_latch <= '0';
      done_latch <= '0';
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        counter <= counter_next;
        burst_stop <= burst_stop_next;
        burst_count <= burst_count_next;
        state <= state_next;
        reset_latch <= reset_latch_next;
        done_latch <= done_latch_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaTblibMemcontrollerlib_c2_sbghByXoFHXTGWdFEAGFXsC_DDR4_Bu_process;

  dnnfpgaTblibMemcontrollerlib_c2_sbghByXoFHXTGWdFEAGFXsC_DDR4_Bu_output : PROCESS (burst_count, burst_stop, counter, done_latch, length_in_unsigned, rd_dvalid,
       reset_1, reset_latch, start, state)
    VARIABLE Done1 : std_logic;
    VARIABLE burst_stop_temp : unsigned(15 DOWNTO 0);
    VARIABLE burst_count_temp : unsigned(15 DOWNTO 0);
    VARIABLE reset_latch_temp : std_logic;
    VARIABLE cast : unsigned(63 DOWNTO 0);
    VARIABLE add_temp : unsigned(32 DOWNTO 0);
    VARIABLE add_temp_0 : unsigned(16 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(0, 33);
    add_temp_0 := to_unsigned(16#00000#, 17);
    cast := to_unsigned(0, 64);
    burst_stop_temp := burst_stop;
    burst_count_temp := burst_count;
    reset_latch_temp := reset_latch;
    counter_next <= counter;
    -- Constant
    -- Create persistent variables (registers)
    -- State Memory Encoding
    IF reset_1 = '1' THEN 
      reset_latch_temp := '1';
    ELSIF start = '1' THEN 
      reset_latch_temp := '0';
    END IF;
    -- Output Functional Logic
    -- 1. Every state MUST assign every output value
    -- 2. Use explicit data type always
    CASE state IS
      WHEN "0000" =>
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#0000#, 16);
        rd_avalid <= '0';
        -- output to DUT
        count_outdeadOut <= to_unsigned(16#0000#, 16);
        valid_out <= '0';
        ready_out <= '1';
        Done1 := '0';
        -- State vars
        counter_next <= to_unsigned(16#00000000#, 32);
        burst_count_temp := to_unsigned(16#0000#, 16);
      WHEN "0001" =>
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#0000#, 16);
        rd_avalid <= '0';
        -- output to DUT
        valid_out <= '0';
        count_outdeadOut <= to_unsigned(16#0000#, 16);
        ready_out <= '1';
        IF reset_latch_temp = '1' AND (counter /= to_unsigned(16#00000000#, 32)) THEN 
          Done1 := '1';
        ELSE 
          Done1 := done_latch AND hdlcoder_to_stdlogic(counter = to_unsigned(16#00000000#, 32));
        END IF;
        -- State vars
        IF length_in_unsigned(31 DOWNTO 16) /= X"0000" THEN 
          burst_stop_temp := X"FFFF";
        ELSE 
          burst_stop_temp := length_in_unsigned(15 DOWNTO 0);
        END IF;
        burst_count_temp := to_unsigned(16#0000#, 16);
        IF reset_1 = '1' THEN 
          counter_next <= to_unsigned(16#00000000#, 32);
        END IF;
      WHEN "0010" =>
        -- output to AXI4 Master
        cast := resize(counter & '0' & '0', 64);
        IF cast(63 DOWNTO 32) /= X"00000000" THEN 
          rd_addr_tmp <= X"FFFFFFFF";
        ELSE 
          rd_addr_tmp <= cast(31 DOWNTO 0);
        END IF;
        rd_len_tmp <= burst_stop;
        rd_avalid <= '1';
        -- output to DUT
        valid_out <= '0';
        count_outdeadOut <= to_unsigned(16#0000#, 16);
        ready_out <= '0';
        Done1 := '0';
      WHEN "0100" =>
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= burst_stop;
        rd_avalid <= '0';
        -- output to DUT
        valid_out <= rd_dvalid;
        count_outdeadOut <= burst_count;
        ready_out <= '0';
        Done1 := '0';
        -- State vars
        IF rd_dvalid = '1' THEN 
          add_temp := resize(counter, 33) + to_unsigned(1, 33);
          IF add_temp(32) /= '0' THEN 
            counter_next <= X"FFFFFFFF";
          ELSE 
            counter_next <= add_temp(31 DOWNTO 0);
          END IF;
          add_temp_0 := resize(burst_count, 17) + to_unsigned(16#00001#, 17);
          IF add_temp_0(16) /= '0' THEN 
            burst_count_temp := X"FFFF";
          ELSE 
            burst_count_temp := add_temp_0(15 DOWNTO 0);
          END IF;
        END IF;
      WHEN OTHERS => 
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#0000#, 16);
        rd_avalid <= '0';
        -- output to DUT
        valid_out <= '0';
        count_outdeadOut <= to_unsigned(16#0000#, 16);
        ready_out <= '0';
        Done1 := '0';
    END CASE;
    -- Next State Logic
    CASE state IS
      WHEN "0000" =>
        state_next <= to_unsigned(16#1#, 4);
      WHEN "0001" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#2#, 4);
        ELSE 
          state_next <= to_unsigned(16#1#, 4);
        END IF;
      WHEN "0010" =>
        state_next <= to_unsigned(16#4#, 4);
      WHEN "0100" =>
        IF burst_count_temp = burst_stop_temp THEN 
          state_next <= to_unsigned(16#1#, 4);
        ELSE 
          state_next <= to_unsigned(16#4#, 4);
        END IF;
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 4);
    END CASE;
    done_latch_next <= Done1;
    rd_ready <= '1';
    state_outdeadOut <= state;
    Done <= Done1;
    burst_stop_next <= burst_stop_temp;
    burst_count_next <= burst_count_temp;
    reset_latch_next <= reset_latch_temp;
  END PROCESS dnnfpgaTblibMemcontrollerlib_c2_sbghByXoFHXTGWdFEAGFXsC_DDR4_Bu_output;


  count_out_tmp <= to_unsigned(16#0000#, 16);

  count_out <= std_logic_vector(count_out_tmp);

  rd_addr <= std_logic_vector(rd_addr_tmp);

  rd_len <= std_logic_vector(rd_len_tmp);

  state_out_tmp <= to_unsigned(16#0#, 4);

  state_out <= std_logic_vector(state_out_tmp);

END rtl;

