-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_SignalWait.vhd
-- Created: 2025-10-22 15:46:43
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_SignalWait
-- Source Path: testbench/DUT/DUT/Conv Module/Real Conv Kernel/ConvEngine/InputProcessor0_T/IPSync/SyncedProcessorDynamicLCLoading/SyncHub/SignalWait
-- Hierarchy Level: 9
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_SignalWait IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        start                             :   IN    std_logic;
        swIn                              :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        wIn                               :   IN    std_logic_vector(11 DOWNTO 0);  -- ufix12
        done                              :   OUT   std_logic;
        sOut                              :   OUT   std_logic_vector(11 DOWNTO 0);  -- ufix12
        stateOut                          :   OUT   std_logic_vector(1 DOWNTO 0)  -- ufix2
        );
END MyProc_src_SignalWait;


ARCHITECTURE rtl OF MyProc_src_SignalWait IS

  -- Functions
  -- HDLCODER_TO_STDLOGIC 
  FUNCTION hdlcoder_to_stdlogic(arg: boolean) RETURN std_logic IS
  BEGIN
    IF arg THEN
      RETURN '1';
    ELSE
      RETURN '0';
    END IF;
  END FUNCTION;


  -- Signals
  SIGNAL swIn_unsigned                    : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL wIn_unsigned                     : unsigned(11 DOWNTO 0);  -- ufix12
  SIGNAL sOut_tmp                         : unsigned(11 DOWNTO 0);  -- ufix12
  SIGNAL stateOutdeadOut                  : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL state                            : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL wInst_reg                        : unsigned(11 DOWNTO 0);  -- ufix12
  SIGNAL wMasked_reg                      : unsigned(11 DOWNTO 0);  -- ufix12
  SIGNAL state_next                       : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL wInst_reg_next                   : unsigned(11 DOWNTO 0);  -- ufix12
  SIGNAL wMasked_reg_next                 : unsigned(11 DOWNTO 0);  -- ufix12
  SIGNAL stateOut_tmp                     : unsigned(1 DOWNTO 0);  -- ufix2

BEGIN
  swIn_unsigned <= unsigned(swIn);

  wIn_unsigned <= unsigned(wIn);

  dnnfpgaSyncSwlib_c2_SignalWait_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      state <= to_unsigned(16#0#, 2);
      wInst_reg <= to_unsigned(16#000#, 12);
      wMasked_reg <= to_unsigned(16#001#, 12);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        state <= state_next;
        wInst_reg <= wInst_reg_next;
        wMasked_reg <= wMasked_reg_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaSyncSwlib_c2_SignalWait_process;

  dnnfpgaSyncSwlib_c2_SignalWait_output : PROCESS (start, state, swIn_unsigned, wIn_unsigned, wInst_reg, wMasked_reg)
    VARIABLE op2 : unsigned(1 DOWNTO 0);
    VARIABLE wAnd : std_logic;
    VARIABLE w : std_logic;
    VARIABLE wInst_reg_temp : unsigned(11 DOWNTO 0);
  BEGIN
    -- add register to achieve better timing performance
    op2 := swIn_unsigned(28 DOWNTO 27);
    IF wInst_reg = wMasked_reg THEN 
      wAnd := '1';
    ELSE 
      wAnd := '0';
    END IF;
    w := hdlcoder_to_stdlogic((wMasked_reg /= to_unsigned(16#000#, 12)) OR (wInst_reg = to_unsigned(16#000#, 12)));
    wInst_reg_temp := swIn_unsigned(26 DOWNTO 15);
    wMasked_reg_next <= wInst_reg_temp AND wIn_unsigned;
    -- OR
    IF op2 = to_unsigned(16#0#, 2) THEN 
      -- AND
      w := wAnd;
    END IF;
    CASE state IS
      WHEN "00" =>
        done <= '0';
      WHEN "01" =>
        done <= '0';
      WHEN "10" =>
        done <= '1';
      WHEN OTHERS => 
        done <= '0';
    END CASE;
    CASE state IS
      WHEN "00" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#1#, 2);
        ELSE 
          state_next <= to_unsigned(16#0#, 2);
        END IF;
      WHEN "01" =>
        IF w = '1' THEN 
          state_next <= to_unsigned(16#2#, 2);
        ELSE 
          state_next <= to_unsigned(16#1#, 2);
        END IF;
      WHEN "10" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#1#, 2);
        ELSE 
          state_next <= to_unsigned(16#2#, 2);
        END IF;
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 2);
    END CASE;
    sOut_tmp <= swIn_unsigned(14 DOWNTO 3);
    stateOutdeadOut <= state;
    wInst_reg_next <= wInst_reg_temp;
  END PROCESS dnnfpgaSyncSwlib_c2_SignalWait_output;


  sOut <= std_logic_vector(sOut_tmp);

  stateOut_tmp <= to_unsigned(16#0#, 2);

  stateOut <= std_logic_vector(stateOut_tmp);

END rtl;

