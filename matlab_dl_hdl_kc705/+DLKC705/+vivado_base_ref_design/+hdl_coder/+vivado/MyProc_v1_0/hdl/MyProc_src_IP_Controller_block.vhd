-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_IP_Controller_block.vhd
-- Created: 2025-10-22 15:46:43
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_IP_Controller_block
-- Source Path: testbench/DUT/DUT/Conv Module/Real Conv Kernel/handShaking/ReadDataFromDDR/IP_Controller
-- Hierarchy Level: 6
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_IP_Controller_block IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        start                             :   IN    std_logic;
        startIP                           :   IN    std_logic;
        rd_dvalid                         :   IN    std_logic;
        len                               :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        rd_avalid                         :   OUT   std_logic;
        doneIP                            :   OUT   std_logic;
        counterOut                        :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        state_out                         :   OUT   std_logic_vector(3 DOWNTO 0)  -- ufix4
        );
END MyProc_src_IP_Controller_block;


ARCHITECTURE rtl OF MyProc_src_IP_Controller_block IS

  -- Signals
  SIGNAL len_unsigned                     : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL doneIPdeadOut                    : std_logic;
  SIGNAL counterOutdeadOut                : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state_outdeadOut                 : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL counter                          : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state                            : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL counter_next                     : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state_next                       : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL counterOut_tmp                   : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state_out_tmp                    : unsigned(3 DOWNTO 0);  -- ufix4

BEGIN
  len_unsigned <= unsigned(len);

  dnnfpgaConv5Module_c16_IP_Controller_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      counter <= to_unsigned(16#00000000#, 32);
      state <= to_unsigned(16#0#, 4);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        counter <= counter_next;
        state <= state_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaConv5Module_c16_IP_Controller_process;

  dnnfpgaConv5Module_c16_IP_Controller_output : PROCESS (counter, len_unsigned, rd_dvalid, start, startIP, state)
    VARIABLE counter_temp : unsigned(31 DOWNTO 0);
    VARIABLE add_temp : unsigned(32 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(0, 33);
    counter_temp := counter;
    -- valid_out is bus signal
    -- state machine
    -- State Memory Encoding
    -- outputs
    -- state definition
    CASE state IS
      WHEN "0000" =>
        rd_avalid <= '0';
        doneIPdeadOut <= '0';
        counter_temp := to_unsigned(16#00000000#, 32);
      WHEN "0001" =>
        rd_avalid <= '0';
        doneIPdeadOut <= '0';
      WHEN "0010" =>
        rd_avalid <= '1';
        doneIPdeadOut <= '0';
        counter_temp := to_unsigned(16#00000000#, 32);
      WHEN "0011" =>
        rd_avalid <= '0';
        doneIPdeadOut <= '0';
        IF rd_dvalid = '1' THEN 
          add_temp := resize(counter, 33) + to_unsigned(1, 33);
          IF add_temp(32) /= '0' THEN 
            counter_temp := X"FFFFFFFF";
          ELSE 
            counter_temp := add_temp(31 DOWNTO 0);
          END IF;
        END IF;
      WHEN "0100" =>
        rd_avalid <= '0';
        doneIPdeadOut <= '1';
        counter_temp := to_unsigned(16#00000000#, 32);
      WHEN OTHERS => 
        rd_avalid <= '0';
        doneIPdeadOut <= '0';
        counter_temp := to_unsigned(16#00000000#, 32);
    END CASE;
    -- Next State Logic
    CASE state IS
      WHEN "0000" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#1#, 4);
        ELSE 
          state_next <= to_unsigned(16#0#, 4);
        END IF;
      WHEN "0001" =>
        IF startIP = '1' THEN 
          state_next <= to_unsigned(16#2#, 4);
        ELSE 
          state_next <= to_unsigned(16#1#, 4);
        END IF;
      WHEN "0010" =>
        state_next <= to_unsigned(16#3#, 4);
      WHEN "0011" =>
        IF counter_temp = len_unsigned THEN 
          state_next <= to_unsigned(16#4#, 4);
        ELSE 
          state_next <= to_unsigned(16#3#, 4);
        END IF;
      WHEN "0100" =>
        state_next <= to_unsigned(16#1#, 4);
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 4);
    END CASE;
    counterOutdeadOut <= counter;
    state_outdeadOut <= state;
    counter_next <= counter_temp;
  END PROCESS dnnfpgaConv5Module_c16_IP_Controller_output;


  doneIP <= '0';

  counterOut_tmp <= to_unsigned(0, 32);

  counterOut <= std_logic_vector(counterOut_tmp);

  state_out_tmp <= to_unsigned(16#0#, 4);

  state_out <= std_logic_vector(state_out_tmp);

END rtl;

