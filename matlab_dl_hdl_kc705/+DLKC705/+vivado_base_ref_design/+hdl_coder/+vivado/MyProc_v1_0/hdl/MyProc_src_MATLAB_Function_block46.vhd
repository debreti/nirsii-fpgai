-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_MATLAB_Function_block46.vhd
-- Created: 2025-10-22 15:46:43
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_MATLAB_Function_block46
-- Source Path: testbench/DUT/DUT/Conv Module/Real Conv Kernel/MATLAB Function
-- Hierarchy Level: 4
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_MATLAB_Function_block46 IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        LegChange                         :   IN    std_logic;
        Start                             :   IN    std_logic;
        Offset                            :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        FinalTileLoading                  :   IN    std_logic;
        LegWeightBaseAddr                 :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        state_out                         :   OUT   std_logic_vector(1 DOWNTO 0);  -- ufix2
        WeightBaseOffset                  :   OUT   std_logic_vector(31 DOWNTO 0)  -- uint32
        );
END MyProc_src_MATLAB_Function_block46;


ARCHITECTURE rtl OF MyProc_src_MATLAB_Function_block46 IS

  -- Signals
  SIGNAL state_out_tmp                    : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL Offset_unsigned                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL LegWeightBaseAddr_unsigned       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state_outdeadOut                 : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL WeightBaseOffset_tmp             : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state                            : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL state_next                       : unsigned(1 DOWNTO 0);  -- ufix2

BEGIN
  state_out_tmp <= to_unsigned(16#0#, 2);

  state_out <= std_logic_vector(state_out_tmp);

  Offset_unsigned <= unsigned(Offset);

  LegWeightBaseAddr_unsigned <= unsigned(LegWeightBaseAddr);

  dnnfpgaConv5Module_c2_MATLAB_Function_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      state <= to_unsigned(16#0#, 2);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        state <= state_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaConv5Module_c2_MATLAB_Function_process;

  dnnfpgaConv5Module_c2_MATLAB_Function_output : PROCESS (FinalTileLoading, LegChange, LegWeightBaseAddr_unsigned, Offset_unsigned,
       Start, state)
  BEGIN
    -- Create persistent variables (registers)
    -- State Memory Encoding
    -- Output Functional Logic
    -- 1. Every state MUST assign every output value
    -- 2. Use explicit data type always
    CASE state IS
      WHEN "00" =>
        WeightBaseOffset_tmp <= LegWeightBaseAddr_unsigned;
      WHEN "01" =>
        WeightBaseOffset_tmp <= LegWeightBaseAddr_unsigned;
      WHEN "10" =>
        WeightBaseOffset_tmp <= Offset_unsigned;
      WHEN OTHERS => 
        WeightBaseOffset_tmp <= Offset_unsigned;
    END CASE;
    -- Next State Logic
    CASE state IS
      WHEN "00" =>
        IF Start = '1' THEN 
          state_next <= to_unsigned(16#1#, 2);
        ELSE 
          state_next <= to_unsigned(16#0#, 2);
        END IF;
      WHEN "01" =>
        IF (Start AND FinalTileLoading) = '1' THEN 
          state_next <= to_unsigned(16#2#, 2);
        ELSIF LegChange = '1' THEN 
          state_next <= to_unsigned(16#0#, 2);
        ELSE 
          state_next <= to_unsigned(16#1#, 2);
        END IF;
      WHEN "10" =>
        IF LegChange = '1' THEN 
          state_next <= to_unsigned(16#0#, 2);
        ELSE 
          state_next <= to_unsigned(16#2#, 2);
        END IF;
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 2);
    END CASE;
    state_outdeadOut <= state;
  END PROCESS dnnfpgaConv5Module_c2_MATLAB_Function_output;


  WeightBaseOffset <= std_logic_vector(WeightBaseOffset_tmp);

END rtl;

