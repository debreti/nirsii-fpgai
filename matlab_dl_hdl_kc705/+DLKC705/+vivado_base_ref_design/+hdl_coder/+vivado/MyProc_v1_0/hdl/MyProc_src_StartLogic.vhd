-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_StartLogic.vhd
-- Created: 2025-10-22 15:46:45
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_StartLogic
-- Source Path: testbench/DUT/DUT/Conv Module/Real Conv Kernel/ConvEngine/CONV/CONV_Compute/Convolver/Subsystem/ConvController/StartLogic
-- Hierarchy Level: 10
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_StartLogic IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        reset_1                           :   IN    std_logic;
        start                             :   IN    std_logic;
        finalTileLoading                  :   IN    std_logic;
        streamingReady                    :   IN    std_logic;
        coefFetchingReady                 :   IN    std_logic;
        streamingStart                    :   OUT   std_logic;
        coefFetchStart                    :   OUT   std_logic;
        coefPreFetchStart                 :   OUT   std_logic;
        ready                             :   OUT   std_logic;
        stateOut                          :   OUT   std_logic_vector(4 DOWNTO 0)  -- ufix5
        );
END MyProc_src_StartLogic;


ARCHITECTURE rtl OF MyProc_src_StartLogic IS

  -- Signals
  SIGNAL stateOutdeadOut                  : unsigned(4 DOWNTO 0);  -- ufix5
  SIGNAL state                            : unsigned(4 DOWNTO 0);  -- ufix5
  SIGNAL state_next                       : unsigned(4 DOWNTO 0);  -- ufix5
  SIGNAL stateOut_tmp                     : unsigned(4 DOWNTO 0);  -- ufix5

BEGIN
  dnnfpgaConvcontroller2lib_c14_StartLogic_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      state <= to_unsigned(16#00#, 5);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        state <= state_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaConvcontroller2lib_c14_StartLogic_process;

  dnnfpgaConvcontroller2lib_c14_StartLogic_output : PROCESS (coefFetchingReady, finalTileLoading, reset_1, start, state, streamingReady)
  BEGIN
    -- coef prefetch is happened for first layer of each leg. Previously we
    -- merge it with the coefFetchStart. Now we seperate them into two
    -- different output signal in order to handle max-pool before conv
    -- issue as this coefPreFetchStart will be discarded if the first layer
    -- of a leg is maxpool/avgpool 
    coefPreFetchStart <= '0';
    CASE state IS
      WHEN "00000" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "00001" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '1';
      WHEN "00010" =>
        streamingStart <= '0';
        coefFetchStart <= '1';
        coefPreFetchStart <= '1';
        ready <= '0';
      WHEN "00011" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "00100" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "00101" =>
        streamingStart <= '1';
        coefFetchStart <= '1';
        ready <= '0';
      WHEN "00110" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "00111" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "01000" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "01001" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "01010" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "01011" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '1';
      WHEN "01100" =>
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
      WHEN "01101" =>
        streamingStart <= '1';
        coefFetchStart <= '1';
        ready <= '0';
      WHEN "01110" =>
        streamingStart <= '1';
        coefFetchStart <= '1';
        ready <= '0';
      WHEN "01111" =>
        streamingStart <= '1';
        coefFetchStart <= '1';
        ready <= '0';
      WHEN "10000" =>
        streamingStart <= '1';
        coefFetchStart <= '1';
        ready <= '0';
      WHEN OTHERS => 
        streamingStart <= '0';
        coefFetchStart <= '0';
        ready <= '0';
    END CASE;
    CASE state IS
      WHEN "00000" =>
        state_next <= to_unsigned(16#01#, 5);
      WHEN "00001" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#02#, 5);
        ELSE 
          state_next <= to_unsigned(16#01#, 5);
        END IF;
      WHEN "00010" =>
        IF ( NOT coefFetchingReady) = '1' THEN 
          state_next <= to_unsigned(16#04#, 5);
        ELSE 
          state_next <= to_unsigned(16#03#, 5);
        END IF;
      WHEN "00011" =>
        state_next <= to_unsigned(16#05#, 5);
      WHEN "00100" =>
        IF coefFetchingReady = '1' THEN 
          state_next <= to_unsigned(16#05#, 5);
        ELSE 
          state_next <= to_unsigned(16#04#, 5);
        END IF;
      WHEN "00101" =>
        state_next <= to_unsigned(16#10#, 5);
        -- 1 transition state to compensate for matchingDelay
      WHEN "00110" =>
        IF coefFetchingReady = '1' THEN 
          state_next <= to_unsigned(16#05#, 5);
        ELSE 
          state_next <= to_unsigned(16#06#, 5);
        END IF;
      WHEN "00111" =>
        IF streamingReady = '1' THEN 
          state_next <= to_unsigned(16#05#, 5);
        ELSE 
          state_next <= to_unsigned(16#07#, 5);
        END IF;
      WHEN "01000" =>
        state_next <= to_unsigned(16#05#, 5);
      WHEN "01001" =>
        IF coefFetchingReady = '1' THEN 
          state_next <= to_unsigned(16#0B#, 5);
        ELSE 
          state_next <= to_unsigned(16#09#, 5);
        END IF;
      WHEN "01010" =>
        IF streamingReady = '1' THEN 
          state_next <= to_unsigned(16#0B#, 5);
        ELSE 
          state_next <= to_unsigned(16#0A#, 5);
        END IF;
      WHEN "01011" =>
        IF reset_1 = '1' THEN 
          IF start = '1' THEN 
            state_next <= to_unsigned(16#02#, 5);
          ELSE 
            state_next <= to_unsigned(16#01#, 5);
          END IF;
        ELSIF start = '1' THEN 
          state_next <= to_unsigned(16#0D#, 5);
        ELSE 
          state_next <= to_unsigned(16#0B#, 5);
        END IF;
      WHEN "01100" =>
        IF (coefFetchingReady AND streamingReady) = '1' THEN 
          state_next <= to_unsigned(16#0B#, 5);
        ELSIF coefFetchingReady = '1' THEN 
          state_next <= to_unsigned(16#0A#, 5);
        ELSIF streamingReady = '1' THEN 
          state_next <= to_unsigned(16#09#, 5);
        ELSE 
          state_next <= to_unsigned(16#0C#, 5);
        END IF;
      WHEN "01101" =>
        state_next <= to_unsigned(16#05#, 5);
      WHEN "01110" =>
        IF ( NOT finalTileLoading) = '1' THEN 
          IF (coefFetchingReady AND streamingReady) = '1' THEN 
            state_next <= to_unsigned(16#08#, 5);
          ELSIF coefFetchingReady = '1' THEN 
            state_next <= to_unsigned(16#07#, 5);
          ELSIF streamingReady = '1' THEN 
            state_next <= to_unsigned(16#06#, 5);
          ELSE 
            state_next <= to_unsigned(16#0E#, 5);
          END IF;
        ELSIF (coefFetchingReady AND streamingReady) = '1' THEN 
          state_next <= to_unsigned(16#0B#, 5);
        ELSIF coefFetchingReady = '1' THEN 
          state_next <= to_unsigned(16#0A#, 5);
        ELSIF streamingReady = '1' THEN 
          state_next <= to_unsigned(16#09#, 5);
        ELSE 
          state_next <= to_unsigned(16#0C#, 5);
        END IF;
      WHEN "01111" =>
        state_next <= to_unsigned(16#0E#, 5);
        -- 1 transition state to compensate for enabled delay
      WHEN "10000" =>
        state_next <= to_unsigned(16#0F#, 5);
        -- 1 transition state to compensate for enabled delay            
      WHEN OTHERS => 
        state_next <= to_unsigned(16#00#, 5);
    END CASE;
    stateOutdeadOut <= state;
  END PROCESS dnnfpgaConvcontroller2lib_c14_StartLogic_output;


  stateOut_tmp <= to_unsigned(16#00#, 5);

  stateOut <= std_logic_vector(stateOut_tmp);

END rtl;

