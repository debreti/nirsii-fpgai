-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_MATLAB_Function_block32.vhd
-- Created: 2025-10-22 15:46:39
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_MATLAB_Function_block32
-- Source Path: testbench/DUT/DUT/Scheduler/Scheduler/ConcatKernel/StateMachine/MATLAB Function
-- Hierarchy Level: 6
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_MATLAB_Function_block32 IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        valid                             :   IN    std_logic;
        canSend                           :   IN    std_logic;
        kernelDone                        :   IN    std_logic;
        payloadIn_oneBit                  :   IN    std_logic;
        deq                               :   OUT   std_logic;
        send                              :   OUT   std_logic;
        kernelStart                       :   OUT   std_logic;
        payload_oneBit                    :   OUT   std_logic
        );
END MyProc_src_MATLAB_Function_block32;


ARCHITECTURE rtl OF MyProc_src_MATLAB_Function_block32 IS

  -- Signals
  SIGNAL state                            : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL kernelStartReg                   : std_logic;
  SIGNAL payloadReg_oneBit                : std_logic;
  SIGNAL state_next                       : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL kernelStartReg_next              : std_logic;
  SIGNAL payloadReg_oneBit_next           : std_logic;

BEGIN
  MATLAB_Function_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      state <= to_unsigned(16#00#, 8);
      kernelStartReg <= '0';
      payloadReg_oneBit <= '0';
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        state <= state_next;
        kernelStartReg <= kernelStartReg_next;
        payloadReg_oneBit <= payloadReg_oneBit_next;
      END IF;
    END IF;
  END PROCESS MATLAB_Function_process;

  MATLAB_Function_output : PROCESS (canSend, kernelDone, kernelStartReg, payloadIn_oneBit, payloadReg_oneBit,
       state, valid)
    VARIABLE state_temp : unsigned(7 DOWNTO 0);
  BEGIN
    state_temp := state;
    deq <= '0';
    send <= '0';
    kernelStartReg_next <= '0';
    payloadReg_oneBit_next <= '0';
    CASE state IS
      WHEN "00000000" =>
        IF valid = '1' THEN 
          deq <= '1';
          kernelStartReg_next <= '1';
          payloadReg_oneBit_next <= payloadIn_oneBit;
          state_temp := to_unsigned(16#01#, 8);
        END IF;
      WHEN "00000001" =>
        state_temp := to_unsigned(16#02#, 8);
      WHEN "00000010" =>
        IF kernelDone = '1' THEN 
          state_temp := to_unsigned(16#03#, 8);
        END IF;
      WHEN "00000011" =>
        IF canSend = '1' THEN 
          state_temp := to_unsigned(16#04#, 8);
        END IF;
      WHEN "00000100" =>
        IF ( NOT kernelDone) = '1' THEN 
          state_temp := to_unsigned(16#00#, 8);
        END IF;
      WHEN OTHERS => 
        NULL;
    END CASE;
    CASE state_temp IS
      WHEN "00000000" =>
        IF valid = '1' THEN 
          deq <= '1';
          kernelStartReg_next <= '1';
          payloadReg_oneBit_next <= payloadIn_oneBit;
        END IF;
      WHEN "00000011" =>
        IF canSend = '1' THEN 
          send <= '1';
        END IF;
      WHEN OTHERS => 
        NULL;
    END CASE;
    kernelStart <= kernelStartReg;
    payload_oneBit <= payloadReg_oneBit;
    state_next <= state_temp;
  END PROCESS MATLAB_Function_output;


END rtl;

