-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_MATLAB_Function_block40.vhd
-- Created: 2025-10-22 15:46:40
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_MATLAB_Function_block40
-- Source Path: testbench/DUT/DUT/Scheduler/Scheduler/DDRInterface/Subsystem/ModifiedForBoth/trackSlack/MATLAB Function
-- Hierarchy Level: 8
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_MATLAB_Function_block40 IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        enq                               :   IN    std_logic;
        len                               :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        deq                               :   IN    std_logic;
        available                         :   OUT   std_logic_vector(31 DOWNTO 0)  -- uint32
        );
END MyProc_src_MATLAB_Function_block40;


ARCHITECTURE rtl OF MyProc_src_MATLAB_Function_block40 IS

  -- Signals
  SIGNAL len_unsigned                     : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL available_tmp                    : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL slackReg                         : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL slackReg_next                    : unsigned(31 DOWNTO 0);  -- uint32

BEGIN
  len_unsigned <= unsigned(len);

  MATLAB_Function_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      slackReg <= to_unsigned(16#00000030#, 32);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        slackReg <= slackReg_next;
      END IF;
    END IF;
  END PROCESS MATLAB_Function_process;

  MATLAB_Function_output : PROCESS (deq, enq, len_unsigned, slackReg)
    VARIABLE used : unsigned(31 DOWNTO 0);
    VARIABLE freed : signed(31 DOWNTO 0);
    VARIABLE add_cast : unsigned(31 DOWNTO 0);
    VARIABLE add_temp : unsigned(32 DOWNTO 0);
    VARIABLE sub_cast : unsigned(31 DOWNTO 0);
    VARIABLE sub_cast_0 : signed(32 DOWNTO 0);
    VARIABLE sub_cast_1 : signed(32 DOWNTO 0);
    VARIABLE sub_temp : signed(32 DOWNTO 0);
  BEGIN
    used := to_unsigned(16#00000000#, 32);
    freed := to_signed(16#00000000#, 32);
    IF enq = '1' THEN 
      used := len_unsigned;
    END IF;
    IF deq = '1' THEN 
      freed := to_signed(16#00000001#, 32);
    END IF;
    add_cast := unsigned(freed);
    add_temp := resize(slackReg, 33) + resize(add_cast, 33);
    IF add_temp(32) /= '0' THEN 
      sub_cast := X"FFFFFFFF";
    ELSE 
      sub_cast := add_temp(31 DOWNTO 0);
    END IF;
    sub_cast_0 := signed(resize(sub_cast, 33));
    sub_cast_1 := signed(resize(used, 33));
    sub_temp := sub_cast_0 - sub_cast_1;
    IF sub_temp(32) = '1' THEN 
      slackReg_next <= X"00000000";
    ELSE 
      slackReg_next <= unsigned(sub_temp(31 DOWNTO 0));
    END IF;
    available_tmp <= slackReg;
  END PROCESS MATLAB_Function_output;


  available <= std_logic_vector(available_tmp);

END rtl;

