-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_MATLAB_Function.vhd
-- Created: 2025-10-22 15:46:34
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_MATLAB_Function
-- Source Path: testbench/DUT/DUT/Scheduler/Scheduler/OutputKernel/StateMachine/MATLAB Function
-- Hierarchy Level: 6
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_MATLAB_Function IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        payloadValid                      :   IN    std_logic;
        payloadIn_addr                    :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        payloadIn_sizeInBytes             :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        payloadIn_id                      :   IN    std_logic_vector(7 DOWNTO 0);  -- uint8
        payloadIn_isLast                  :   IN    std_logic;
        canSend                           :   IN    std_logic;
        frameCountIn                      :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        initIn                            :   IN    std_logic;
        frameBufferCountIn                :   IN    std_logic_vector(15 DOWNTO 0);  -- uint16
        consumedIn                        :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        outputSizeIn                      :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        deq                               :   OUT   std_logic;
        send                              :   OUT   std_logic;
        addrOffset                        :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        addrOffsetPre                     :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        done                              :   OUT   std_logic;
        provided                          :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        state                             :   OUT   std_logic_vector(7 DOWNTO 0);  -- uint8
        totalCount                        :   OUT   std_logic_vector(31 DOWNTO 0)  -- uint32
        );
END MyProc_src_MATLAB_Function;


ARCHITECTURE rtl OF MyProc_src_MATLAB_Function IS

  -- Signals
  SIGNAL payloadIn_addr_unsigned          : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL payloadIn_sizeInBytes_unsigned   : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL payloadIn_id_unsigned            : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL frameCountIn_unsigned            : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL frameBufferCountIn_unsigned      : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL consumedIn_unsigned              : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL outputSizeIn_unsigned            : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL addrOffset_tmp                   : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL addrOffsetPre_tmp                : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL provided_tmp                     : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL statedeadOut                     : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL totalCount_tmp                   : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL stateReg                         : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL addrOffsetReg                    : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL addrOffsetPreReg                 : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL initReg                          : std_logic;
  SIGNAL frameCountReg                    : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL frameBufferCountReg              : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL streamingCountReg                : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL totalCountReg                    : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL consumedReg                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL providedReg                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL outputSizeReg                    : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL stateReg_next                    : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL addrOffsetReg_next               : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL addrOffsetPreReg_next            : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL initReg_next                     : std_logic;
  SIGNAL frameCountReg_next               : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL frameBufferCountReg_next         : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL streamingCountReg_next           : unsigned(15 DOWNTO 0);  -- uint16
  SIGNAL totalCountReg_next               : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL consumedReg_next                 : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL providedReg_next                 : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL outputSizeReg_next               : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state_tmp                        : unsigned(7 DOWNTO 0);  -- uint8

BEGIN
  payloadIn_addr_unsigned <= unsigned(payloadIn_addr);

  payloadIn_sizeInBytes_unsigned <= unsigned(payloadIn_sizeInBytes);

  payloadIn_id_unsigned <= unsigned(payloadIn_id);

  frameCountIn_unsigned <= unsigned(frameCountIn);

  frameBufferCountIn_unsigned <= unsigned(frameBufferCountIn);

  consumedIn_unsigned <= unsigned(consumedIn);

  outputSizeIn_unsigned <= unsigned(outputSizeIn);

  MATLAB_Function_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      stateReg <= to_unsigned(16#00#, 8);
      addrOffsetReg <= to_unsigned(16#00000000#, 32);
      addrOffsetPreReg <= to_unsigned(16#00000000#, 32);
      initReg <= '0';
      frameCountReg <= to_unsigned(16#00000000#, 32);
      frameBufferCountReg <= to_unsigned(16#0000#, 16);
      streamingCountReg <= to_unsigned(16#0000#, 16);
      totalCountReg <= to_unsigned(16#00000000#, 32);
      consumedReg <= to_unsigned(16#00000000#, 32);
      providedReg <= to_unsigned(16#00000000#, 32);
      outputSizeReg <= to_unsigned(16#00000000#, 32);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        stateReg <= stateReg_next;
        addrOffsetReg <= addrOffsetReg_next;
        addrOffsetPreReg <= addrOffsetPreReg_next;
        initReg <= initReg_next;
        frameCountReg <= frameCountReg_next;
        frameBufferCountReg <= frameBufferCountReg_next;
        streamingCountReg <= streamingCountReg_next;
        totalCountReg <= totalCountReg_next;
        consumedReg <= consumedReg_next;
        providedReg <= providedReg_next;
        outputSizeReg <= outputSizeReg_next;
      END IF;
    END IF;
  END PROCESS MATLAB_Function_process;

  MATLAB_Function_output : PROCESS (addrOffsetPreReg, addrOffsetReg, canSend, consumedIn_unsigned, consumedReg,
       frameBufferCountIn_unsigned, frameBufferCountReg, frameCountIn_unsigned,
       frameCountReg, initIn, initReg, outputSizeIn_unsigned, outputSizeReg,
       payloadIn_isLast, payloadValid, providedReg, stateReg, streamingCountReg,
       totalCountReg)
    VARIABLE slots : unsigned(31 DOWNTO 0);
    VARIABLE next_rsvd : unsigned(15 DOWNTO 0);
    VARIABLE add_temp : unsigned(32 DOWNTO 0);
    VARIABLE add_temp_0 : unsigned(32 DOWNTO 0);
    VARIABLE add_temp_1 : unsigned(16 DOWNTO 0);
    VARIABLE add_temp_2 : unsigned(32 DOWNTO 0);
    VARIABLE add_temp_3 : unsigned(32 DOWNTO 0);
  BEGIN
    next_rsvd := to_unsigned(16#0000#, 16);
    add_temp_0 := to_unsigned(0, 33);
    add_temp_1 := to_unsigned(16#00000#, 17);
    add_temp_2 := to_unsigned(0, 33);
    add_temp_3 := to_unsigned(0, 33);
    stateReg_next <= stateReg;
    addrOffsetReg_next <= addrOffsetReg;
    addrOffsetPreReg_next <= addrOffsetPreReg;
    streamingCountReg_next <= streamingCountReg;
    totalCountReg_next <= totalCountReg;
    providedReg_next <= providedReg;
    deq <= '0';
    send <= '0';
    done <= '0';
    add_temp := resize(consumedReg, 33) + resize(frameBufferCountReg, 33);
    IF add_temp(32) /= '0' THEN 
      slots := X"FFFFFFFF";
    ELSE 
      slots := add_temp(31 DOWNTO 0);
    END IF;
    CASE stateReg IS
      WHEN "00000000" =>
        IF (payloadValid AND ( NOT initReg)) = '1' AND (slots > providedReg) THEN 
          IF payloadIn_isLast = '1' THEN 
            add_temp_0 := resize(providedReg, 33) + to_unsigned(1, 33);
            IF add_temp_0(32) /= '0' THEN 
              providedReg_next <= X"FFFFFFFF";
            ELSE 
              providedReg_next <= add_temp_0(31 DOWNTO 0);
            END IF;
            add_temp_1 := resize(streamingCountReg, 17) + to_unsigned(16#00001#, 17);
            IF add_temp_1(16) /= '0' THEN 
              next_rsvd := X"FFFF";
            ELSE 
              next_rsvd := add_temp_1(15 DOWNTO 0);
            END IF;
            IF frameBufferCountReg = streamingCountReg THEN 
              next_rsvd := to_unsigned(16#0001#, 16);
            END IF;
            add_temp_2 := resize(totalCountReg, 33) + to_unsigned(1, 33);
            IF add_temp_2(32) /= '0' THEN 
              totalCountReg_next <= X"FFFFFFFF";
            ELSE 
              totalCountReg_next <= add_temp_2(31 DOWNTO 0);
            END IF;
            streamingCountReg_next <= next_rsvd;
            addrOffsetPreReg_next <= addrOffsetReg;
            IF next_rsvd = frameBufferCountReg THEN 
              addrOffsetReg_next <= to_unsigned(16#00000000#, 32);
            ELSE 
              add_temp_3 := resize(addrOffsetReg, 33) + resize(outputSizeReg, 33);
              IF add_temp_3(32) /= '0' THEN 
                addrOffsetReg_next <= X"FFFFFFFF";
              ELSE 
                addrOffsetReg_next <= add_temp_3(31 DOWNTO 0);
              END IF;
            END IF;
          END IF;
          deq <= '1';
          stateReg_next <= to_unsigned(16#01#, 8);
        END IF;
      WHEN "00000001" =>
        IF (canSend AND ( NOT initReg)) = '1' THEN 
          stateReg_next <= to_unsigned(16#00#, 8);
          send <= '1';
          IF totalCountReg = frameCountReg THEN 
            done <= '1';
          ELSE 
            done <= '0';
          END IF;
        END IF;
      WHEN OTHERS => 
        NULL;
    END CASE;
    IF initReg = '1' THEN 
      stateReg_next <= to_unsigned(16#00#, 8);
      addrOffsetReg_next <= to_unsigned(16#00000000#, 32);
      addrOffsetPreReg_next <= addrOffsetReg;
      streamingCountReg_next <= to_unsigned(16#0000#, 16);
      totalCountReg_next <= to_unsigned(16#00000000#, 32);
      providedReg_next <= to_unsigned(16#00000000#, 32);
    END IF;
    initReg_next <= initIn;
    frameCountReg_next <= frameCountIn_unsigned;
    frameBufferCountReg_next <= frameBufferCountIn_unsigned;
    consumedReg_next <= consumedIn_unsigned;
    outputSizeReg_next <= outputSizeIn_unsigned;
    addrOffset_tmp <= addrOffsetReg;
    addrOffsetPre_tmp <= addrOffsetPreReg;
    provided_tmp <= providedReg;
    statedeadOut <= stateReg;
    totalCount_tmp <= totalCountReg;
  END PROCESS MATLAB_Function_output;


  addrOffset <= std_logic_vector(addrOffset_tmp);

  addrOffsetPre <= std_logic_vector(addrOffsetPre_tmp);

  provided <= std_logic_vector(provided_tmp);

  state_tmp <= to_unsigned(16#00#, 8);

  state <= std_logic_vector(state_tmp);

  totalCount <= std_logic_vector(totalCount_tmp);

END rtl;

