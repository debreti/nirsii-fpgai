-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_MATLAB_Function_block39.vhd
-- Created: 2025-10-22 15:46:40
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_MATLAB_Function_block39
-- Source Path: testbench/DUT/DUT/Scheduler/Scheduler/DDRInterface/MATLAB Function
-- Hierarchy Level: 5
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_MATLAB_Function_block39 IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        vIn                               :   IN    std_logic;
        init                              :   IN    std_logic;
        vOut                              :   OUT   std_logic
        );
END MyProc_src_MATLAB_Function_block39;


ARCHITECTURE rtl OF MyProc_src_MATLAB_Function_block39 IS

  -- Signals
  SIGNAL stateReg                         : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL stateReg_next                    : unsigned(7 DOWNTO 0);  -- uint8

BEGIN
  MATLAB_Function_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      stateReg <= to_unsigned(16#00#, 8);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        stateReg <= stateReg_next;
      END IF;
    END IF;
  END PROCESS MATLAB_Function_process;

  MATLAB_Function_output : PROCESS (init, stateReg, vIn)
  BEGIN
    stateReg_next <= stateReg;
    vOut <= '0';
    CASE stateReg IS
      WHEN "00000000" =>
        IF (vIn AND ( NOT init)) = '1' THEN 
          stateReg_next <= to_unsigned(16#01#, 8);
        END IF;
        IF init = '1' THEN 
          stateReg_next <= to_unsigned(16#00#, 8);
        END IF;
      WHEN "00000001" =>
        IF (( NOT vIn) AND ( NOT init)) = '1' THEN 
          stateReg_next <= to_unsigned(16#02#, 8);
        END IF;
        IF init = '1' THEN 
          stateReg_next <= to_unsigned(16#00#, 8);
        END IF;
      WHEN "00000010" =>
        vOut <= '1';
        IF init = '1' THEN 
          stateReg_next <= to_unsigned(16#00#, 8);
        END IF;
      WHEN OTHERS => 
        NULL;
    END CASE;
  END PROCESS MATLAB_Function_output;


END rtl;

