-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_CoefPrefetchingEmulationDelayLogic.vhd
-- Created: 2025-10-22 15:46:44
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_CoefPrefetchingEmulationDelayLogic
-- Source Path: testbench/DUT/DUT/Conv Module/Real Conv Kernel/ConvEngine/CONV/CONV_Compute/Convolver/CoefPrefetchingEmulationDelayLogic/CoefPrefetchingEmulationDelayLogic
-- Hierarchy Level: 9
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_CoefPrefetchingEmulationDelayLogic IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        readyIn                           :   IN    std_logic;
        delay                             :   IN    std_logic_vector(1 DOWNTO 0);  -- ufix2
        readyOut                          :   OUT   std_logic
        );
END MyProc_src_CoefPrefetchingEmulationDelayLogic;


ARCHITECTURE rtl OF MyProc_src_CoefPrefetchingEmulationDelayLogic IS

  -- Signals
  SIGNAL delay_unsigned                   : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL state                            : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL delayCounter                     : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL state_next                       : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL delayCounter_next                : unsigned(1 DOWNTO 0);  -- ufix2

BEGIN
  delay_unsigned <= unsigned(delay);

  dnnfpgaConvdut2Convolver_lib_c327_CoefPrefetchingEmulationDelay_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      state <= to_unsigned(16#0#, 4);
      delayCounter <= to_unsigned(16#0#, 2);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        state <= state_next;
        delayCounter <= delayCounter_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaConvdut2Convolver_lib_c327_CoefPrefetchingEmulationDelay_process;

  dnnfpgaConvdut2Convolver_lib_c327_CoefPrefetchingEmulationDelay_output : PROCESS (delayCounter, delay_unsigned, readyIn, state)
    VARIABLE add_temp : unsigned(2 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(16#0#, 3);
    delayCounter_next <= delayCounter;
    --     switch (state)
    --         case 0
    --             readyOut = false;
    --         case 1
    --             readyOut = false;
    --             delayCounter = fi(0,0,ceil(log2(delayLimit))+1,0);
    --         case 2
    --             readyOut = false;
    --             delayCounter = fi(delayCounter + 1,0,ceil(log2(delayLimit))+1,0);
    --         case 3
    --             readyOut = true;
    --         otherwise
    --             readyOut = false;
    --     end
    -- 
    CASE state IS
      WHEN "0000" =>
        state_next <= to_unsigned(16#3#, 4);
        readyOut <= '1';
      WHEN "0001" =>
        IF readyIn = '1' THEN 
          state_next <= to_unsigned(16#2#, 4);
          readyOut <= '0';
        ELSE 
          state_next <= to_unsigned(16#1#, 4);
          readyOut <= '0';
          delayCounter_next <= to_unsigned(16#0#, 2);
        END IF;
      WHEN "0010" =>
        IF delayCounter = delay_unsigned THEN 
          state_next <= to_unsigned(16#3#, 4);
          readyOut <= '1';
        ELSE 
          state_next <= to_unsigned(16#2#, 4);
          readyOut <= '0';
          add_temp := resize(delayCounter, 3) + to_unsigned(16#1#, 3);
          IF add_temp(2) /= '0' THEN 
            delayCounter_next <= "11";
          ELSE 
            delayCounter_next <= add_temp(1 DOWNTO 0);
          END IF;
        END IF;
      WHEN "0011" =>
        IF ( NOT readyIn) = '1' THEN 
          state_next <= to_unsigned(16#1#, 4);
          readyOut <= '0';
          delayCounter_next <= to_unsigned(16#0#, 2);
        ELSE 
          state_next <= to_unsigned(16#3#, 4);
          readyOut <= '1';
        END IF;
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 4);
        readyOut <= '0';
    END CASE;
  END PROCESS dnnfpgaConvdut2Convolver_lib_c327_CoefPrefetchingEmulationDelay_output;


END rtl;

