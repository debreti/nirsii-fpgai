-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_MATLAB_Function_block54.vhd
-- Created: 2025-10-22 15:46:49
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_MATLAB_Function_block54
-- Source Path: testbench/DUT/DUT/Subsystem/MATLAB Function
-- Hierarchy Level: 3
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_MATLAB_Function_block54 IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        ipStart                           :   IN    std_logic;
        opDone                            :   IN    std_logic;
        DLStart                           :   OUT   std_logic_vector(31 DOWNTO 0)  -- uint32
        );
END MyProc_src_MATLAB_Function_block54;


ARCHITECTURE rtl OF MyProc_src_MATLAB_Function_block54 IS

  -- Signals
  SIGNAL DLStart_tmp                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL stateReg                         : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL stateReg_next                    : unsigned(7 DOWNTO 0);  -- uint8

BEGIN
  MATLAB_Function_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      stateReg <= to_unsigned(16#00#, 8);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        stateReg <= stateReg_next;
      END IF;
    END IF;
  END PROCESS MATLAB_Function_process;

  MATLAB_Function_output : PROCESS (ipStart, opDone, stateReg)
    VARIABLE DLStart1 : signed(31 DOWNTO 0);
  BEGIN
    stateReg_next <= stateReg;
    DLStart1 := to_signed(16#00000000#, 32);
    CASE stateReg IS
      WHEN "00000000" =>
        IF ipStart = '1' THEN 
          stateReg_next <= to_unsigned(16#01#, 8);
        ELSE 
          stateReg_next <= to_unsigned(16#00#, 8);
        END IF;
      WHEN "00000001" =>
        DLStart1 := to_signed(16#00000001#, 32);
        IF opDone = '1' THEN 
          stateReg_next <= to_unsigned(16#00#, 8);
        ELSE 
          stateReg_next <= to_unsigned(16#01#, 8);
        END IF;
      WHEN OTHERS => 
        NULL;
    END CASE;
    DLStart_tmp <= unsigned(DLStart1);
  END PROCESS MATLAB_Function_output;


  DLStart <= std_logic_vector(DLStart_tmp);

END rtl;

