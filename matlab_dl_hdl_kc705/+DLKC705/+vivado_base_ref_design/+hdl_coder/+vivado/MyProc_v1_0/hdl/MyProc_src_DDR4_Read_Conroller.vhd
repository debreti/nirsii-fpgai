-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_DDR4_Read_Conroller.vhd
-- Created: 2025-10-22 15:46:49
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_DDR4_Read_Conroller
-- Source Path: testbench/DUT/DUT/Subsystem/CNN5_debug1/DMA/DDR4 Read Conroller
-- Hierarchy Level: 5
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_DDR4_Read_Conroller IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        length_in                         :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        start                             :   IN    std_logic;
        rd_dvalid                         :   IN    std_logic;
        rd_ack                            :   IN    std_logic;
        valid_out                         :   OUT   std_logic;
        count_out                         :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        ready_out                         :   OUT   std_logic;
        rd_addr                           :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        rd_len                            :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        rd_avalid                         :   OUT   std_logic;
        rd_ready                          :   OUT   std_logic
        );
END MyProc_src_DDR4_Read_Conroller;


ARCHITECTURE rtl OF MyProc_src_DDR4_Read_Conroller IS

  -- Signals
  SIGNAL length_in_unsigned               : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL count_outdeadOut                 : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL rd_addr_tmp                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL rd_len_tmp                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL burst_stop                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL burst_count                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state                            : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL burst_stop_next                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL burst_count_next                 : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state_next                       : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL count_out_tmp                    : unsigned(31 DOWNTO 0);  -- uint32

BEGIN
  length_in_unsigned <= unsigned(length_in);

  dnnfpgaTblibMemcontrollerlib_c3_DDR4_Read_Conroller_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      burst_stop <= to_unsigned(16#00000000#, 32);
      burst_count <= to_unsigned(16#00000000#, 32);
      state <= to_unsigned(16#0#, 4);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        burst_stop <= burst_stop_next;
        burst_count <= burst_count_next;
        state <= state_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaTblibMemcontrollerlib_c3_DDR4_Read_Conroller_process;

  dnnfpgaTblibMemcontrollerlib_c3_DDR4_Read_Conroller_output : PROCESS (burst_count, burst_stop, length_in_unsigned, rd_dvalid, start, state)
    VARIABLE burst_stop_temp : unsigned(31 DOWNTO 0);
    VARIABLE burst_count_temp : unsigned(31 DOWNTO 0);
    VARIABLE add_temp : unsigned(32 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(0, 33);
    burst_stop_temp := burst_stop;
    burst_count_temp := burst_count;
    -- Create persistent variables (registers)
    -- State Memory Encoding
    -- Output Functional Logic
    -- 1. Every state MUST assign every output value
    -- 2. Use explicit data type always
    CASE state IS
      WHEN "0000" =>
        -- output to AXI4 Master
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        -- output to DUT
        count_outdeadOut <= to_unsigned(16#00000000#, 32);
        valid_out <= '0';
        ready_out <= '1';
        -- State vars
        burst_count_temp := to_unsigned(16#00000000#, 32);
      WHEN "0001" =>
        -- output to AXI4 Master
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        -- output to DUT
        valid_out <= '0';
        count_outdeadOut <= to_unsigned(16#00000000#, 32);
        ready_out <= '1';
        -- State vars
        burst_stop_temp := length_in_unsigned;
        burst_count_temp := to_unsigned(16#00000000#, 32);
      WHEN "0010" =>
        -- output to AXI4 Master
        rd_len_tmp <= burst_stop;
        rd_avalid <= '1';
        -- output to DUT
        valid_out <= '0';
        count_outdeadOut <= to_unsigned(16#00000000#, 32);
        ready_out <= '0';
      WHEN "0100" =>
        -- output to AXI4 Master
        rd_len_tmp <= burst_stop;
        rd_avalid <= '0';
        -- output to DUT
        valid_out <= rd_dvalid;
        count_outdeadOut <= burst_count;
        ready_out <= '0';
        -- State vars
        IF rd_dvalid = '1' THEN 
          add_temp := resize(burst_count, 33) + to_unsigned(1, 33);
          IF add_temp(32) /= '0' THEN 
            burst_count_temp := X"FFFFFFFF";
          ELSE 
            burst_count_temp := add_temp(31 DOWNTO 0);
          END IF;
        END IF;
      WHEN OTHERS => 
        -- output to AXI4 Master
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        -- output to DUT
        valid_out <= '0';
        count_outdeadOut <= to_unsigned(16#00000000#, 32);
        ready_out <= '0';
    END CASE;
    -- Next State Logic
    CASE state IS
      WHEN "0000" =>
        state_next <= to_unsigned(16#1#, 4);
      WHEN "0001" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#2#, 4);
        ELSE 
          state_next <= to_unsigned(16#1#, 4);
        END IF;
      WHEN "0010" =>
        state_next <= to_unsigned(16#4#, 4);
      WHEN "0100" =>
        IF burst_count_temp = burst_stop_temp THEN 
          state_next <= to_unsigned(16#1#, 4);
        ELSE 
          state_next <= to_unsigned(16#4#, 4);
        END IF;
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 4);
    END CASE;
    rd_addr_tmp <= to_unsigned(16#00000000#, 32);
    rd_ready <= '1';
    burst_stop_next <= burst_stop_temp;
    burst_count_next <= burst_count_temp;
  END PROCESS dnnfpgaTblibMemcontrollerlib_c3_DDR4_Read_Conroller_output;


  count_out_tmp <= to_unsigned(0, 32);

  count_out <= std_logic_vector(count_out_tmp);

  rd_addr <= std_logic_vector(rd_addr_tmp);

  rd_len <= std_logic_vector(rd_len_tmp);

END rtl;

