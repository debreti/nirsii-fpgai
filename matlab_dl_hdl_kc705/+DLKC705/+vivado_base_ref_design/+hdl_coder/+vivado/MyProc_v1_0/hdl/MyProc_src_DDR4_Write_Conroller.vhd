-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_DDR4_Write_Conroller.vhd
-- Created: 2025-10-22 15:46:41
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_DDR4_Write_Conroller
-- Source Path: testbench/DUT/DUT/FC Kernel/Real FC Kernel/FCEngine/DDR4 Write Conroller
-- Hierarchy Level: 5
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_DDR4_Write_Conroller IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        length_in                         :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        start                             :   IN    std_logic;
        wr_ready                          :   IN    std_logic;
        wr_complete                       :   IN    std_logic;
        ram_addr_valid                    :   OUT   std_logic;
        ram_addr                          :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        dma_write_ready                   :   OUT   std_logic;
        wr_addr                           :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        wr_len                            :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        wr_valid                          :   OUT   std_logic;
        stateOut                          :   OUT   std_logic_vector(3 DOWNTO 0)  -- ufix4
        );
END MyProc_src_DDR4_Write_Conroller;


ARCHITECTURE rtl OF MyProc_src_DDR4_Write_Conroller IS

  -- Signals
  SIGNAL length_in_unsigned               : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL ram_addr_tmp                     : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL dma_write_readydeadOut           : std_logic;
  SIGNAL wr_addr_tmp                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL wr_len_tmp                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL stateOutdeadOut                  : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL burst_stop                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL burst_count                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state                            : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL burst_stop_next                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL burst_count_next                 : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL state_next                       : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL stateOut_tmp                     : unsigned(3 DOWNTO 0);  -- ufix4

BEGIN
  length_in_unsigned <= unsigned(length_in);

  dnnfpgaFCModuleCNN5_c344_DDR4_Write_Conroller_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      burst_stop <= to_unsigned(16#00000000#, 32);
      burst_count <= to_unsigned(16#00000000#, 32);
      state <= to_unsigned(16#0#, 4);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        burst_stop <= burst_stop_next;
        burst_count <= burst_count_next;
        state <= state_next;
      END IF;
    END IF;
  END PROCESS dnnfpgaFCModuleCNN5_c344_DDR4_Write_Conroller_process;

  dnnfpgaFCModuleCNN5_c344_DDR4_Write_Conroller_output : PROCESS (burst_count, burst_stop, length_in_unsigned, start, state, wr_complete,
       wr_ready)
    VARIABLE burst_stop_temp : unsigned(31 DOWNTO 0);
    VARIABLE burst_count_temp : unsigned(31 DOWNTO 0);
    VARIABLE add_temp : unsigned(32 DOWNTO 0);
    VARIABLE add_temp_0 : unsigned(32 DOWNTO 0);
    VARIABLE add_temp_1 : unsigned(32 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(0, 33);
    add_temp_0 := to_unsigned(0, 33);
    add_temp_1 := to_unsigned(0, 33);
    burst_stop_temp := burst_stop;
    burst_count_temp := burst_count;
    CASE state IS
      WHEN "0000" =>
        wr_len_tmp <= to_unsigned(16#00000000#, 32);
        wr_valid <= '0';
        ram_addr_valid <= '0';
        ram_addr_tmp <= to_unsigned(16#00000000#, 32);
        dma_write_readydeadOut <= '1';
        burst_stop_temp := length_in_unsigned;
        burst_count_temp := to_unsigned(16#00000000#, 32);
      WHEN "0001" =>
        wr_len_tmp <= to_unsigned(16#00000000#, 32);
        wr_valid <= '0';
        ram_addr_valid <= '1';
        ram_addr_tmp <= to_unsigned(16#00000000#, 32);
        dma_write_readydeadOut <= '0';
        add_temp := resize(burst_count, 33) + to_unsigned(1, 33);
        IF add_temp(32) /= '0' THEN 
          burst_count_temp := X"FFFFFFFF";
        ELSE 
          burst_count_temp := add_temp(31 DOWNTO 0);
        END IF;
      WHEN "0010" =>
        wr_len_tmp <= burst_stop;
        wr_valid <= '1';
        ram_addr_valid <= wr_ready;
        ram_addr_tmp <= burst_count;
        dma_write_readydeadOut <= '0';
        IF wr_ready = '1' THEN 
          add_temp_1 := resize(burst_count, 33) + to_unsigned(1, 33);
          IF add_temp_1(32) /= '0' THEN 
            burst_count_temp := X"FFFFFFFF";
          ELSE 
            burst_count_temp := add_temp_1(31 DOWNTO 0);
          END IF;
        END IF;
      WHEN "0011" =>
        wr_len_tmp <= burst_stop;
        wr_valid <= '0';
        ram_addr_valid <= '0';
        ram_addr_tmp <= burst_count;
        dma_write_readydeadOut <= '0';
      WHEN "0100" =>
        wr_len_tmp <= burst_stop;
        wr_valid <= '0';
        ram_addr_valid <= '1';
        ram_addr_tmp <= burst_count;
        dma_write_readydeadOut <= '0';
        add_temp_0 := resize(burst_count, 33) + to_unsigned(1, 33);
        IF add_temp_0(32) /= '0' THEN 
          burst_count_temp := X"FFFFFFFF";
        ELSE 
          burst_count_temp := add_temp_0(31 DOWNTO 0);
        END IF;
      WHEN "0101" =>
        wr_len_tmp <= burst_stop;
        wr_valid <= '1';
        ram_addr_valid <= '0';
        ram_addr_tmp <= to_unsigned(16#00000000#, 32);
        dma_write_readydeadOut <= '0';
      WHEN "0110" =>
        wr_len_tmp <= to_unsigned(16#00000000#, 32);
        wr_valid <= '0';
        ram_addr_valid <= '0';
        ram_addr_tmp <= to_unsigned(16#00000000#, 32);
        dma_write_readydeadOut <= '0';
      WHEN OTHERS => 
        wr_len_tmp <= to_unsigned(16#00000000#, 32);
        wr_valid <= '0';
        ram_addr_valid <= '0';
        ram_addr_tmp <= to_unsigned(16#00000000#, 32);
        dma_write_readydeadOut <= '0';
    END CASE;
    CASE state IS
      WHEN "0000" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#1#, 4);
        ELSE 
          state_next <= to_unsigned(16#0#, 4);
        END IF;
      WHEN "0001" =>
        IF burst_count_temp = burst_stop_temp THEN 
          state_next <= to_unsigned(16#5#, 4);
        ELSE 
          state_next <= to_unsigned(16#2#, 4);
        END IF;
      WHEN "0010" =>
        IF burst_count_temp = burst_stop_temp THEN 
          state_next <= to_unsigned(16#5#, 4);
        ELSIF wr_ready = '1' THEN 
          state_next <= to_unsigned(16#2#, 4);
        ELSE 
          state_next <= to_unsigned(16#3#, 4);
        END IF;
      WHEN "0011" =>
        IF wr_ready = '1' THEN 
          state_next <= to_unsigned(16#4#, 4);
        ELSE 
          state_next <= to_unsigned(16#3#, 4);
        END IF;
      WHEN "0100" =>
        IF burst_count_temp = burst_stop_temp THEN 
          state_next <= to_unsigned(16#5#, 4);
        ELSE 
          state_next <= to_unsigned(16#2#, 4);
        END IF;
      WHEN "0101" =>
        state_next <= to_unsigned(16#6#, 4);
      WHEN "0110" =>
        IF wr_complete = '1' THEN 
          state_next <= to_unsigned(16#0#, 4);
        ELSE 
          state_next <= to_unsigned(16#6#, 4);
        END IF;
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 4);
    END CASE;
    wr_addr_tmp <= to_unsigned(16#00000000#, 32);
    stateOutdeadOut <= state;
    burst_stop_next <= burst_stop_temp;
    burst_count_next <= burst_count_temp;
  END PROCESS dnnfpgaFCModuleCNN5_c344_DDR4_Write_Conroller_output;


  ram_addr <= std_logic_vector(ram_addr_tmp);

  dma_write_ready <= '0';

  wr_addr <= std_logic_vector(wr_addr_tmp);

  wr_len <= std_logic_vector(wr_len_tmp);

  stateOut_tmp <= to_unsigned(16#0#, 4);

  stateOut <= std_logic_vector(stateOut_tmp);

END rtl;

