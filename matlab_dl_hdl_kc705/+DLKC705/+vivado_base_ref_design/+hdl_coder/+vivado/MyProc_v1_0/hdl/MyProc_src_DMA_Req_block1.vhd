-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_DMA_Req_block1.vhd
-- Created: 2025-10-22 15:46:49
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_DMA_Req_block1
-- Source Path: testbench/DUT/DUT/DDR Read Arbitrator1/DMA_Req
-- Hierarchy Level: 3
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_DMA_Req_block1 IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        start                             :   IN    std_logic;
        length                            :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        offset                            :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        arid                              :   IN    std_logic_vector(7 DOWNTO 0);  -- uint8
        times_counter                     :   IN    std_logic_vector(1 DOWNTO 0);  -- ufix2
        rd_aready_delayed                 :   IN    std_logic;
        done_out                          :   OUT   std_logic;
        rd_addr                           :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        rd_len                            :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        rd_avalid                         :   OUT   std_logic;
        rd_arid                           :   OUT   std_logic_vector(7 DOWNTO 0);  -- uint8
        rd_ready                          :   OUT   std_logic;
        req_counter_out                   :   OUT   std_logic_vector(1 DOWNTO 0);  -- ufix2
        state_out                         :   OUT   std_logic_vector(3 DOWNTO 0)  -- ufix4
        );
END MyProc_src_DMA_Req_block1;


ARCHITECTURE rtl OF MyProc_src_DMA_Req_block1 IS

  -- Signals
  SIGNAL length_unsigned                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL offset_unsigned                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL arid_unsigned                    : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL times_counter_unsigned           : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL rd_addr_tmp                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL rd_len_tmp                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL rd_arid_tmp                      : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL rd_readydeadOut                  : std_logic;
  SIGNAL req_counter_out_tmp              : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL state_out_tmp                    : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL req_counter                      : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL length_reg                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL offset_reg                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL arid_reg                         : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL state                            : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL req_counter_next                 : unsigned(1 DOWNTO 0);  -- ufix2
  SIGNAL length_reg_next                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL offset_reg_next                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL arid_reg_next                    : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL state_next                       : unsigned(3 DOWNTO 0);  -- ufix4

BEGIN
  length_unsigned <= unsigned(length);

  offset_unsigned <= unsigned(offset);

  arid_unsigned <= unsigned(arid);

  times_counter_unsigned <= unsigned(times_counter);

  DMA_Req_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      req_counter <= to_unsigned(16#0#, 2);
      length_reg <= to_unsigned(16#00000000#, 32);
      offset_reg <= to_unsigned(16#00000000#, 32);
      arid_reg <= to_unsigned(16#00#, 8);
      state <= to_unsigned(16#0#, 4);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        req_counter <= req_counter_next;
        length_reg <= length_reg_next;
        offset_reg <= offset_reg_next;
        arid_reg <= arid_reg_next;
        state <= state_next;
      END IF;
    END IF;
  END PROCESS DMA_Req_process;

  DMA_Req_output : PROCESS (arid_reg, arid_unsigned, length_reg, length_unsigned, offset_reg,
       offset_unsigned, rd_aready_delayed, req_counter, start, state,
       times_counter_unsigned)
    VARIABLE add_temp : unsigned(2 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(16#0#, 3);
    req_counter_next <= req_counter;
    length_reg_next <= length_reg;
    offset_reg_next <= offset_reg;
    arid_reg_next <= arid_reg;
    -- valid_out is bus signal
    -- request counter, counts from 0 to NUM_OF_INPUTS - 1
    -- state machine
    -- State Memory Encoding
    -- outputs
    -- state transition
    -- Output Functional Logic
    -- 1. Every state MUST assign every output value
    -- 2. Use explicit data type always
    CASE state IS
      WHEN "0000" =>
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        rd_arid_tmp <= to_unsigned(16#00#, 8);
        -- other outputs
        done_out <= '0';
        -- State vars
        req_counter_next <= to_unsigned(16#0#, 2);
        length_reg_next <= to_unsigned(16#00000000#, 32);
        offset_reg_next <= to_unsigned(16#00000000#, 32);
        arid_reg_next <= to_unsigned(16#00#, 8);
      WHEN "0001" =>
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        rd_arid_tmp <= to_unsigned(16#00#, 8);
        -- other outputs
        done_out <= '0';
        -- State vars
        IF req_counter = to_unsigned(16#3#, 2) THEN 
          req_counter_next <= to_unsigned(16#0#, 2);
        ELSE 
          add_temp := resize(req_counter, 3) + to_unsigned(16#1#, 3);
          IF add_temp(2) /= '0' THEN 
            req_counter_next <= "11";
          ELSE 
            req_counter_next <= add_temp(1 DOWNTO 0);
          END IF;
        END IF;
        length_reg_next <= to_unsigned(16#00000000#, 32);
        offset_reg_next <= to_unsigned(16#00000000#, 32);
        arid_reg_next <= to_unsigned(16#00#, 8);
      WHEN "0010" =>
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        rd_arid_tmp <= to_unsigned(16#00#, 8);
        -- other outputs
        done_out <= '0';
        -- State vars
        length_reg_next <= length_unsigned;
        offset_reg_next <= offset_unsigned;
        arid_reg_next <= arid_unsigned;
      WHEN "0011" =>
        -- output to AXI4 Master
        rd_addr_tmp <= offset_reg;
        rd_len_tmp <= length_reg;
        rd_avalid <= '1';
        rd_arid_tmp <= arid_reg;
        -- other outputs
        done_out <= '0';
        -- State vars
      WHEN "0100" =>
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        rd_arid_tmp <= to_unsigned(16#00#, 8);
        -- other outputs
        done_out <= '1';
        -- State vars
        length_reg_next <= to_unsigned(16#00000000#, 32);
        offset_reg_next <= to_unsigned(16#00000000#, 32);
        arid_reg_next <= to_unsigned(16#00#, 8);
      WHEN "0101" =>
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        rd_arid_tmp <= to_unsigned(16#00#, 8);
        -- other outputs
        done_out <= '0';
        -- State vars
        length_reg_next <= to_unsigned(16#00000000#, 32);
        offset_reg_next <= to_unsigned(16#00000000#, 32);
        arid_reg_next <= to_unsigned(16#00#, 8);
      WHEN OTHERS => 
        -- output to AXI4 Master
        rd_addr_tmp <= to_unsigned(16#00000000#, 32);
        rd_len_tmp <= to_unsigned(16#00000000#, 32);
        rd_avalid <= '0';
        rd_arid_tmp <= to_unsigned(16#00#, 8);
        -- other outputs
        done_out <= '0';
        -- State vars
        req_counter_next <= to_unsigned(16#0#, 2);
        length_reg_next <= to_unsigned(16#00000000#, 32);
        offset_reg_next <= to_unsigned(16#00000000#, 32);
        arid_reg_next <= to_unsigned(16#00#, 8);
    END CASE;
    -- Next State Logic
    CASE state IS
      WHEN "0000" =>
        state_next <= to_unsigned(16#1#, 4);
      WHEN "0001" =>
        state_next <= to_unsigned(16#2#, 4);
      WHEN "0010" =>
        IF start = '1' THEN 
          IF rd_aready_delayed = '1' THEN 
            state_next <= to_unsigned(16#3#, 4);
          ELSE 
            state_next <= to_unsigned(16#2#, 4);
          END IF;
        ELSE 
          state_next <= to_unsigned(16#1#, 4);
        END IF;
      WHEN "0011" =>
        state_next <= to_unsigned(16#4#, 4);
      WHEN "0100" =>
        state_next <= to_unsigned(16#5#, 4);
      WHEN "0101" =>
        IF times_counter_unsigned < to_unsigned(16#3#, 2) THEN 
          state_next <= to_unsigned(16#1#, 4);
        ELSE 
          state_next <= to_unsigned(16#5#, 4);
        END IF;
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 4);
    END CASE;
    rd_readydeadOut <= '1';
    req_counter_out_tmp <= req_counter;
    state_out_tmp <= state;
  END PROCESS DMA_Req_output;


  rd_addr <= std_logic_vector(rd_addr_tmp);

  rd_len <= std_logic_vector(rd_len_tmp);

  rd_arid <= std_logic_vector(rd_arid_tmp);

  rd_ready <= '0';

  req_counter_out <= std_logic_vector(req_counter_out_tmp);

  state_out <= std_logic_vector(state_out_tmp);

END rtl;

