-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_DMA_wrReq.vhd
-- Created: 2025-10-22 15:46:50
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_DMA_wrReq
-- Source Path: testbench/DUT/DUT/DDR Write Arbitrator1/DMA_wrReq
-- Hierarchy Level: 3
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_DMA_wrReq IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        start                             :   IN    std_logic;
        length                            :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        offset                            :   IN    std_logic_vector(31 DOWNTO 0);  -- uint32
        valid                             :   IN    std_logic;
        wr_complete                       :   IN    std_logic;
        wr_addr                           :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        wr_len                            :   OUT   std_logic_vector(31 DOWNTO 0);  -- uint32
        wr_valid                          :   OUT   std_logic;
        req_counter_out                   :   OUT   std_logic_vector(2 DOWNTO 0);  -- ufix3
        send                              :   OUT   std_logic;
        done                              :   OUT   std_logic;
        reset_1                           :   OUT   std_logic;
        state_out                         :   OUT   std_logic_vector(3 DOWNTO 0)  -- ufix4
        );
END MyProc_src_DMA_wrReq;


ARCHITECTURE rtl OF MyProc_src_DMA_wrReq IS

  -- Signals
  SIGNAL length_unsigned                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL offset_unsigned                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL wr_addr_tmp                      : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL wr_len_tmp                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL wr_validdeadOut                  : std_logic;
  SIGNAL req_counter_out_tmp              : unsigned(2 DOWNTO 0);  -- ufix3
  SIGNAL reset_2                          : std_logic;
  SIGNAL state_outdeadOut                 : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL req_counter                      : unsigned(2 DOWNTO 0);  -- ufix3
  SIGNAL length_reg                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL offset_reg                       : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL valid_reg                        : std_logic;
  SIGNAL state                            : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL req_counter_next                 : unsigned(2 DOWNTO 0);  -- ufix3
  SIGNAL length_reg_next                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL offset_reg_next                  : unsigned(31 DOWNTO 0);  -- uint32
  SIGNAL valid_reg_next                   : std_logic;
  SIGNAL state_next                       : unsigned(3 DOWNTO 0);  -- ufix4
  SIGNAL state_out_tmp                    : unsigned(3 DOWNTO 0);  -- ufix4

BEGIN
  length_unsigned <= unsigned(length);

  offset_unsigned <= unsigned(offset);

  DMA_wrReq_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      req_counter <= to_unsigned(16#0#, 3);
      length_reg <= to_unsigned(16#00000000#, 32);
      offset_reg <= to_unsigned(16#00000000#, 32);
      valid_reg <= '0';
      state <= to_unsigned(16#0#, 4);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        req_counter <= req_counter_next;
        length_reg <= length_reg_next;
        offset_reg <= offset_reg_next;
        valid_reg <= valid_reg_next;
        state <= state_next;
      END IF;
    END IF;
  END PROCESS DMA_wrReq_process;

  DMA_wrReq_output : PROCESS (length_reg, length_unsigned, offset_reg, offset_unsigned, req_counter, start,
       state, valid, valid_reg, wr_complete)
    VARIABLE add_temp : unsigned(3 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(16#0#, 4);
    req_counter_next <= req_counter;
    -- request counter, counts from 0 to NUM_OF_INPUTS - 1
    -- state machine
    -- State Memory Encoding
    -- outputs
    -- state transition
    -- Output Functional Logic
    -- 1. Every state MUST assign every output value
    -- 2. Use explicit data type always
    CASE state IS
      WHEN "0000" =>
        -- output to AXI4 Master
        wr_addr_tmp <= to_unsigned(16#00000000#, 32);
        wr_len_tmp <= to_unsigned(16#00000000#, 32);
        wr_validdeadOut <= '0';
        reset_2 <= '1';
        send <= '0';
        done <= '0';
        -- State vars
        req_counter_next <= to_unsigned(16#0#, 3);
        length_reg_next <= to_unsigned(16#00000000#, 32);
        offset_reg_next <= to_unsigned(16#00000000#, 32);
        valid_reg_next <= '0';
      WHEN "0001" =>
        -- output to AXI4 Master
        wr_addr_tmp <= to_unsigned(16#00000000#, 32);
        wr_len_tmp <= to_unsigned(16#00000000#, 32);
        wr_validdeadOut <= '0';
        reset_2 <= '0';
        send <= '0';
        done <= '0';
        -- State vars
        IF req_counter = to_unsigned(16#4#, 3) THEN 
          req_counter_next <= to_unsigned(16#0#, 3);
        ELSE 
          add_temp := resize(req_counter, 4) + to_unsigned(16#1#, 4);
          IF add_temp(3) /= '0' THEN 
            req_counter_next <= "111";
          ELSE 
            req_counter_next <= add_temp(2 DOWNTO 0);
          END IF;
        END IF;
        length_reg_next <= length_unsigned;
        offset_reg_next <= offset_unsigned;
        valid_reg_next <= '0';
      WHEN "0010" =>
        -- output to AXI4 Master
        wr_addr_tmp <= to_unsigned(16#00000000#, 32);
        wr_len_tmp <= to_unsigned(16#00000000#, 32);
        wr_validdeadOut <= '0';
        reset_2 <= '0';
        send <= '0';
        done <= '0';
        -- State vars
        length_reg_next <= length_unsigned;
        offset_reg_next <= offset_unsigned;
        valid_reg_next <= '0';
      WHEN "0011" =>
        -- output to AXI4 Master
        wr_addr_tmp <= offset_reg;
        wr_len_tmp <= length_reg;
        wr_validdeadOut <= valid_reg;
        reset_2 <= '0';
        send <= '1';
        done <= '0';
        -- State vars
        length_reg_next <= length_unsigned;
        offset_reg_next <= offset_unsigned;
        valid_reg_next <= valid;
      WHEN "0100" =>
        -- output to AXI4 Master
        wr_addr_tmp <= offset_reg;
        wr_len_tmp <= length_reg;
        wr_validdeadOut <= '0';
        reset_2 <= '0';
        send <= '0';
        done <= '1';
        -- State vars
        length_reg_next <= length_unsigned;
        offset_reg_next <= offset_unsigned;
        valid_reg_next <= valid;
      WHEN OTHERS => 
        -- output to AXI4 Master
        wr_addr_tmp <= to_unsigned(16#00000000#, 32);
        wr_len_tmp <= to_unsigned(16#00000000#, 32);
        wr_validdeadOut <= '0';
        reset_2 <= '0';
        send <= '0';
        done <= '0';
        -- State vars
        req_counter_next <= to_unsigned(16#0#, 3);
        length_reg_next <= to_unsigned(16#00000000#, 32);
        offset_reg_next <= to_unsigned(16#00000000#, 32);
        valid_reg_next <= '0';
    END CASE;
    -- Next State Logic
    CASE state IS
      WHEN "0000" =>
        state_next <= to_unsigned(16#1#, 4);
      WHEN "0001" =>
        state_next <= to_unsigned(16#2#, 4);
      WHEN "0010" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#3#, 4);
        ELSE 
          state_next <= to_unsigned(16#1#, 4);
        END IF;
      WHEN "0011" =>
        IF wr_complete = '1' THEN 
          state_next <= to_unsigned(16#4#, 4);
        ELSE 
          state_next <= to_unsigned(16#3#, 4);
        END IF;
      WHEN "0100" =>
        state_next <= to_unsigned(16#1#, 4);
      WHEN OTHERS => 
        state_next <= to_unsigned(16#0#, 4);
    END CASE;
    req_counter_out_tmp <= req_counter;
    state_outdeadOut <= state;
  END PROCESS DMA_wrReq_output;


  wr_addr <= std_logic_vector(wr_addr_tmp);

  wr_len <= std_logic_vector(wr_len_tmp);

  wr_valid <= '0';

  req_counter_out <= std_logic_vector(req_counter_out_tmp);

  state_out_tmp <= to_unsigned(16#0#, 4);

  state_out <= std_logic_vector(state_out_tmp);

  reset_1 <= reset_2;

END rtl;

