-- -------------------------------------------------------------
-- 
-- File Name: customProc\hdlsrc\testbench\MyProc_src_MATLAB_Function_block33.vhd
-- Created: 2025-10-22 15:46:39
-- 
-- Generated by MATLAB 24.1, HDL Coder 24.1, and Simulink 24.1
-- 
-- -------------------------------------------------------------


-- -------------------------------------------------------------
-- 
-- Module: MyProc_src_MATLAB_Function_block33
-- Source Path: testbench/DUT/DUT/Scheduler/Scheduler/ConcatKernel/KernelCore/MATLAB Function
-- Hierarchy Level: 6
-- Model version: 8.28
-- 
-- -------------------------------------------------------------
LIBRARY IEEE;
USE IEEE.std_logic_1164.ALL;
USE IEEE.numeric_std.ALL;

ENTITY MyProc_src_MATLAB_Function_block33 IS
  PORT( clk                               :   IN    std_logic;
        reset                             :   IN    std_logic;
        enb                               :   IN    std_logic;
        start                             :   IN    std_logic;
        payloadIn_oneBit                  :   IN    std_logic;
        done                              :   OUT   std_logic;
        payload_oneBit                    :   OUT   std_logic;
        running                           :   OUT   std_logic
        );
END MyProc_src_MATLAB_Function_block33;


ARCHITECTURE rtl OF MyProc_src_MATLAB_Function_block33 IS

  -- Signals
  SIGNAL payloaddeadOut_oneBit            : std_logic;
  SIGNAL runningdeadOut                   : std_logic;
  SIGNAL state                            : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL payloadReg_oneBit                : std_logic;
  SIGNAL count                            : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL state_next                       : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL payloadReg_oneBit_next           : std_logic;
  SIGNAL count_next                       : unsigned(7 DOWNTO 0);  -- uint8
  SIGNAL payload                          : std_logic;

BEGIN
  MATLAB_Function_process : PROCESS (clk, reset)
  BEGIN
    IF reset = '1' THEN
      state <= to_unsigned(16#00#, 8);
      payloadReg_oneBit <= '0';
      count <= to_unsigned(16#00#, 8);
    ELSIF clk'EVENT AND clk = '1' THEN
      IF enb = '1' THEN
        state <= state_next;
        payloadReg_oneBit <= payloadReg_oneBit_next;
        count <= count_next;
      END IF;
    END IF;
  END PROCESS MATLAB_Function_process;

  MATLAB_Function_output : PROCESS (count, payloadIn_oneBit, payloadReg_oneBit, start, state)
    VARIABLE add_temp : unsigned(8 DOWNTO 0);
  BEGIN
    add_temp := to_unsigned(16#000#, 9);
    state_next <= state;
    payloadReg_oneBit_next <= payloadReg_oneBit;
    count_next <= count;
    done <= '0';
    runningdeadOut <= '0';
    CASE state IS
      WHEN "00000000" =>
        IF start = '1' THEN 
          state_next <= to_unsigned(16#01#, 8);
        END IF;
      WHEN "00000001" =>
        runningdeadOut <= '1';
        state_next <= to_unsigned(16#02#, 8);
        payloadReg_oneBit_next <= payloadIn_oneBit;
      WHEN "00000010" =>
        runningdeadOut <= '1';
        IF count = to_unsigned(16#00000000#, 8) THEN 
          state_next <= to_unsigned(16#03#, 8);
        END IF;
        add_temp := resize(count, 9) + to_unsigned(16#001#, 9);
        IF add_temp(8) /= '0' THEN 
          count_next <= "11111111";
        ELSE 
          count_next <= add_temp(7 DOWNTO 0);
        END IF;
      WHEN "00000011" =>
        count_next <= to_unsigned(16#00#, 8);
        done <= '1';
        state_next <= to_unsigned(16#00#, 8);
      WHEN OTHERS => 
        NULL;
    END CASE;
    payloaddeadOut_oneBit <= payloadReg_oneBit;
  END PROCESS MATLAB_Function_output;


  payload <= '0';

  payload_oneBit <= payload;

  running <= '0';

END rtl;

